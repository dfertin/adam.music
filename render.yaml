version: "1"
services:
  - type: web
    name: adam-music-app
    env: node
    buildCommand: "npm run install:all && npm run build"
    startCommand: "npm start"
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
